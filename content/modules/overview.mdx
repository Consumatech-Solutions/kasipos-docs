---
title: Modules Overview
description: Extending KasiPOS with modules.
fullWidth: true
---

<div className="min-h-screen bg-[hsl(var(--background))] text-[hsl(var(--foreground))]">
  <div className="mx-auto flex max-w-[1400px]">

    <main className="min-h-screen flex-1 px-6 pb-20 pt-24 md:px-12 lg:pl-64 xl:pl-64 xl:pr-64">
      <div className="mx-auto max-w-[720px]">
        <header className="mb-10">
          <h1 className="mb-4 text-[40px] font-extrabold tracking-tight">Modules Overview</h1>
          <p className="text-[17px] leading-relaxed text-[hsl(var(--foreground))/0.7]">
            KasiPOS supports module-based extensions for custom business workflows.
          </p>
          <p className="mt-3 text-[15px] text-[hsl(var(--foreground))/0.65]">
            This section provides a high-level module map for documentation and onboarding.
          </p>
        </header>

        <section className="space-y-6" id="module-map">
          <section id="checkout-sales">
            <h2 className="text-lg font-semibold">Checkout &amp; Sales</h2>
            <p className="mt-1 text-[14px] text-[hsl(var(--foreground))/0.7]">
              Includes core checkout and sales operations: <span className="font-mono">/sale</span>, <span className="font-mono">/transactions</span>.
            </p>
          </section>
          <section id="inventory-catalogue">
            <h2 className="text-lg font-semibold">Inventory &amp; Catalogue</h2>
            <p className="mt-1 text-[14px] text-[hsl(var(--foreground))/0.7]">
              Product and stock management workflows: <span className="font-mono">/inventory</span>, <span className="font-mono">/catalogue</span>.
            </p>
          </section>
          <section id="customers-vouchers">
            <h2 className="text-lg font-semibold">Customers &amp; Vouchers</h2>
            <p className="mt-1 text-[14px] text-[hsl(var(--foreground))/0.7]">
              Customer context and promotions: <span className="font-mono">/customers</span>, <span className="font-mono">/vouchers</span>.
            </p>
          </section>
          <section id="procurement-marketplace">
            <h2 className="text-lg font-semibold">Procurement &amp; Marketplace</h2>
            <p className="mt-1 text-[14px] text-[hsl(var(--foreground))/0.7]">
              Stock buying and supplier/store flows: <span className="font-mono">/buy-stock</span>, <span className="font-mono">/buy-stock/cart</span>, <span className="font-mono">/buy-stock/history</span>, <span className="font-mono">/marketplace</span>, <span className="font-mono">/marketplace/[storeId]</span>, <span className="font-mono">/marketplace/orders</span>.
            </p>
          </section>
          <section id="reports">
            <h2 className="text-lg font-semibold">Reports</h2>
            <p className="mt-1 text-[14px] text-[hsl(var(--foreground))/0.7]">
              Monitoring dashboards and business insights: <span className="font-mono">/reports</span>.
            </p>
          </section>
          <section id="settings">
            <h2 className="text-lg font-semibold">Settings</h2>
            <p className="mt-1 text-[14px] text-[hsl(var(--foreground))/0.7]">
              Operational and platform configuration: <span className="font-mono">/settings</span>.
            </p>
          </section>
        </section>
      </div>
    </main>

    <aside className="fixed right-[max(0px,calc(50%-700px))] top-14 hidden h-[calc(100vh-3.5rem)] w-64 overflow-y-auto px-8 py-10 xl:block">
      <h4 className="mb-6 text-[11px] font-bold uppercase tracking-widest text-[hsl(var(--foreground))/0.45]">On this page</h4>
      <nav className="flex flex-col gap-3">
        <a className="-ml-[2px] border-l-2 border-[hsl(var(--primary))] pl-4 text-[13px] font-medium" href="#module-map">Module Map</a>
      </nav>
    </aside>
  </div>
</div>
